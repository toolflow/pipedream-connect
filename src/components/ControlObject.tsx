type ControlObjectProps = {
  component?: React.ComponentType<{
    id: string;
    name: string;
    value: string;
  }>;
};

export function ControlObject(props: ControlObjectProps) {
  return <></>;
}
